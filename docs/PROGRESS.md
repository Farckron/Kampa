# PROGRESS

One line per completed task: date · stage · task · commit · note.

- 2026-07-25 · planning · SPEC.md, CLAUDE.md, stages/, research report written · — · design approved section-by-section in brainstorming session
- 2026-07-25 · stage01 · Astro project scaffold · 5fa46e1 · minimal Astro 7 template in repo root, sample content cleaned out.
- 2026-07-25 · stage01 · Config — site/base, Tailwind v4, React, sitemap · 7eb1fe1 · `@tailwindcss/vite` + `@astrojs/react` + sitemap wired; SITE/BASE left as documented placeholders pending the user's repo.
- 2026-07-25 · stage01 · Base layout with CSP + canonical · 4143bf3 · `Base.astro` html shell with SPEC §2.3 CSP meta tag and canonical derived from `site`.
- 2026-07-25 · stage01 · Static plumbing — robots.txt, llms.txt, 404 · 1db45c5 (+fix 6719346) · crawl plumbing and 404 page added; follow-up made the 404 home link base-aware.
- 2026-07-25 · stage01 · Test toolchains — vitest + Playwright config · 9ee7cff · vitest with one passing smoke test, Playwright config only (no e2e tests yet).
- 2026-07-25 · stage01 · shadcn init + MCP + Prettier · e14c289 (+fix 289971d) · shadcn initialised into `src/components/ui/`, `.mcp.json` server and Prettier config added; follow-up moved build-only shadcn tooling to devDependencies.
- 2026-07-25 · stage01 · GitHub Actions deploy workflow · 4455ecf · push-to-main → withastro/action → deploy-pages; live-URL verification still blocked on the user's repo.
- 2026-07-25 · stage02 · Landing shell — Nav, Hero, Footer composed in `index.astro` · a337c5a · CSS-only mobile menu, shared `max-w-6xl px-6` container, SPEC §3.1/§3.8 copy verbatim, no horizontal scroll 360→1440px, empty `#how-it-works` anchor left for stage 03.
- 2026-07-25 · stage02 · Tier-1 guard e2e spec · 0c50faa · Playwright asserts zero `<script` tags and zero third-party requests on `/`; webServer now runs `build && preview`.
- 2026-07-25 · stage03 · Sample plans + `samples` content collection · 468b815 · three hand-written 90-day plans behind a glob loader + zod schema; `/samples` index and `/samples/[slug]` pages with hand-styled markdown (no typography plugin) and tables that scroll inside the table box.
- 2026-07-25 · stage03 · Tier-1 guard spec covers JSON-LD and more pages · 4a9e75c · zero-`<script` assertion replaced with "every `<script` must be `application/ld+json`"; both guards now sweep `/`, `/faq` and a sample page.
- 2026-07-25 · stage03 · FAQ accordion, /faq, /guide/api-key, /privacy, /terms · 88fd420 · native `<details>` accordion with the 6 approved items + FAQPage JSON-LD; API-key guide as styled step cards (no screenshots) with HowTo JSON-LD.
- 2026-07-25 · stage03 · Landing composed, OG meta, llms.txt · a821176 · Hero→HowItWorks→Features→Comparison→Samples→Faq→FinalCta; CSS-only radio tabs in Samples fed by the collection; SoftwareApplication JSON-LD; static `public/og.png` hero screenshot wired into `og:image`/`twitter:card`; no horizontal scroll at 360/768/1440.
- 2026-07-25 · stage03 · Review fixes — sample budgets, FAQ schema/h1, open-source link · — · sample calendars now schedule every euro the budget tables claim (coffee 12 boosts / 3 shoots / 3 tastings / 2 flyer runs, salon 9 x €40 boosts, boutique 3 shoots + 12 parcels); FAQPage JSON-LD emitted only from `/faq`, which gained its own `<h1>`; "open source" now links to `src/lib/site.ts`'s single `GITHUB_URL`.
- 2026-07-25 · stage03 · Lighthouse recorded · — · Lighthouse 13.4.1 `--preset=desktop` on the local preview: `/`, `/faq`, `/samples/riga-coffee-shop` all 100/100/100/100. Deploy half of the stage gate still blocked on the user's repo (as with stage 01), so "deployed" is unverified.
- 2026-07-25 · stage04 · Wizard shell, context, mock generation engine · c88ddb3 · `WizardContext` feeds the stage-04 reducer to the island; `Wizard.tsx` owns the header (wordmark, model badge, Clear key), `?demo=1` / stored-key + draft restore on mount and the phase switch; `mock.ts` resolves each stage off the demo fixture after ~800ms and prices usage from `MODELS` (~€0.15/€0.20/€0.45, matching the estimates the generation screen shows). Phase components reconciled as a set; fixed two `tabs.tsx` bugs the result screen exposed (orientation attribute, `min-w-0`).
- 2026-07-25 · stage04 · `/app` page + CSP moved into Astro config · 2f6951a · `<Wizard client:only="react">` in `Base` with no Nav/Footer and `noindex`; the hand-written CSP meta became `security.csp` so Astro hashes its own inline hydration bootstrap — same SPEC §2.3 policy, inline styles narrowed to `style-src-attr` for Radix.
- 2026-07-25 · stage04 · Wizard e2e + mock cost unit test · 5a4a3b8 · demo mode, the full mocked happy path (key → 8 intake steps → 3 generate clicks → result) and clear-key-returns-to-gate, each asserting nothing leaves the origin; tier1 landing guards untouched. Gate: build ok, 26 unit tests, `astro check` 0 errors, 5 e2e passing; no horizontal scroll at 360/768/1280 (the calendar table scrolls inside its own box).
- 2026-07-25 · stage05 · Anthropic client, cost table, prompt engine · 4942074 · `src/lib/anthropic.ts` streams `POST /v1/messages` over plain fetch (SPEC §2.4 headers, line-buffered SSE that survives mid-line chunk splits, usage from message_start/message_delta, 401/429/529/400 + `refusal` mapped to owner-readable messages, key never leaves a local); `src/lib/cost.ts` prices from the one `MODELS` table with cache read/write factors and per-stage estimates; `src/lib/prompts/` holds the cached >1024-token system prompt, the three JSON schemas with hand-rolled validators, and the stage builders (copy batched 4 weeks at a time).
- 2026-07-25 · stage05 · Real generation wired with streaming, errors and constraint checks · d087467 · `engine.ts` `runStageReal` reads the key at call time, dispatches STAGE_START → ADD_USAGE → STAGE_DONE, retries exactly once with the validation error fed back, and on failure leaves the stage pending with no partial data; `checkBudget`/`checkHours` flag a split that misses the budget or a week over the hours ceiling. Wizard runs the real engine for key mode and keeps demo mode on the bundled fixture, with a dismissible error banner (rate-limit countdown, "Re-enter key" on auth). Generation shows the streamed text live and per-stage amber warnings with a Regenerate shortcut; estimates now come from `cost.ts`. Two additive actions (`STAGE_FAIL`, `TO_GATE`) — the stage-04 reducer could only clear `running` via STAGE_DONE, so any error froze the screen.
- 2026-07-25 · stage05 · Engine unit tests + intercepted-API e2e · 05b96ce · `tests/unit/engine.test.ts` mocks `streamMessage` for the dispatch sequence, the retry path, ApiError handling, copy batching and the constraint-check edges; `tests/e2e/wizard.spec.ts` now drives the real engine against a routed `api.anthropic.com` serving recorded SSE, asserting the streamed preview renders, the exact headers, no temperature/top_p/top_k, no assistant prefill, the €0.05 meter, and zero requests to any other host — plus a 401 path that shows the friendly banner and leaves the stage pending. Gate: build ok, 90 unit tests, `astro check` 0 errors, 6 e2e passing.
- 2026-07-25 · stage05 · Review fixes — budget unit, 5xx retry, intake caching · — · the intake asks for a MONTHLY budget (SPEC §4.2 q4) and the plan covers 90 days, so `budget90()` is now the single source of that number: the intake block states "1200 EUR total for the 90 days (400 EUR per month)", the stage-1 task asks the split to sum to the same figure, `checkBudget()` validates against it, and the budget question says the plan spends three months of it. `generate()` retries once on `overloaded`/`rate_limit` (honouring retry-after) per SPEC §2.4, spent once per stage call so it cannot compound with the validation retry into four requests. The user message is now `UserBlock[]`: intake (cached) + strategy JSON (cached, stages 2-3) + the task block, so the cached prefix covers system + intake + strategy instead of stopping at the system prompt.
